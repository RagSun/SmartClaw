# SmartClaw 运行时依赖（pyproject 核心 deps 之外，`smartclaw start` 实际运行所需）
#
# 背景：pyproject.toml 的 [project].dependencies 只含 Web/CLI 框架，未含 Agent 执行内核
# （deepagents + LangChain/LangGraph）与 Redis 客户端，因此 `uv pip install -e .` 产出的镜像
# 能构建但无法真正启动。本文件补齐运行时闭包，版本对齐已验证的本地环境，保证可复现。
#
# 用法：Dockerfile 中 `uv pip install -r requirements-runtime.txt`（见 Dockerfile）。

# —— Agent 执行内核 ——
deepagents==0.5.5
langchain==1.2.16
langchain-core==1.3.2
langgraph==1.1.10
langgraph-checkpoint==4.0.3
langgraph-prebuilt==1.0.13
langsmith==0.7.38

# —— LLM Provider 适配（按需启用，导入期需可用）——
langchain-openai==1.2.1

# —— 运行时基础设施 ——
redis>=5.0
tiktoken==0.12.0
orjson>=3.10
tenacity>=8.0

# —— 记忆数据面 PostgreSQL 后端（store=postgres 时使用；sqlite 默认不依赖）——
psycopg[binary,pool]>=3.1
